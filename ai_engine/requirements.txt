# PyTorch: use the official CPU wheel index to avoid CUDA runtime dependencies.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0

# The application imports the focused LangChain packages below directly; the
# umbrella `langchain` package is intentionally omitted.
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
chromadb==1.5.9
numpy==1.26.4
onnxruntime==1.20.1
fastapi==0.139.2
starlette==1.3.1
requests
beautifulsoup4
sentence-transformers
python-dotenv
python-pptx==1.0.2

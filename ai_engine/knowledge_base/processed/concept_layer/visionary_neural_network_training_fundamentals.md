# 神经网络训练基础

## 池化层的选择

最大池化（max pooling）保留局部窗口中的最大响应，适合突出边缘、纹理等显著特征；平均池化（average pooling）计算窗口均值，更强调区域的整体统计并让响应更平滑。两者都能降低特征图的空间尺寸，但保留信息的侧重点不同。

## 梯度消失与梯度爆炸

反向传播需要按链式法则让梯度逐层通过网络。多层导数连续相乘后趋近于零时会发生梯度消失，靠近输入的层几乎无法学习；连续相乘后数值过大时会发生梯度爆炸，参数更新和损失会变得不稳定。合适的参数初始化、归一化、残差连接以及梯度裁剪可用于缓解这些问题。

